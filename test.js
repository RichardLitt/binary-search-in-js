/* globals it */
'use strict'

var assert = require('assert')
var binSearch = require('./')

// Need a different testing framework or something

it('should ', function () {
  assert.strictEqual(binSearch('), '')
})
